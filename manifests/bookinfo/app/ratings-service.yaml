# Copyright Istio Authors
# Licensed under the Apache License, Version 2.0
apiVersion: v1
kind: Service
metadata:
  name: ratings
  namespace: bookinfo
  # Change this annotation to modify visibility across mesh boundaries.
  annotations:
    networking.istio.io/exportTo: '*'
  labels:
    app: ratings
    service: ratings
spec:
  ports:
  - port: 9080
    name: http
  selector:
    app: ratings
