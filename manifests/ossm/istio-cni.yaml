# Apply to both clusters. Requires ISTIO_VERSION env var (use envsubst).
apiVersion: sailoperator.io/v1alpha1
kind: IstioCNI
metadata:
  name: default
spec:
  version: v${ISTIO_VERSION}
  namespace: istio-cni
