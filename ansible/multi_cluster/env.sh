# Local shell helpers (optional). This file is gitignored — do not commit secrets.
# The multi-cluster playbook does not source env.sh. Configure kubeconfig first:
# see repository README.md §1.1 (East) and §1.2 (West): oc login, rename-context
# to admin-east / admin-west, optional oc apply for console-notification.yaml.
